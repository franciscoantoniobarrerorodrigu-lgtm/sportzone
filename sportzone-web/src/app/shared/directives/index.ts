/**
 * Barrel export para directivas compartidas
 * 
 * Permite importar todas las directivas desde un solo punto:
 * import { LazyLoadDirective } from '@shared/directives';
 */

export { LazyLoadDirective } from './lazy-load.directive';
